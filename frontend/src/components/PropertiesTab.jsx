import { Box, useColorModeValue } from "@chakra-ui/react";
import React from "react";

const PropertiesTab = (props) => {
	return <Box className="properties-tab">This is properties</Box>;
};

export default PropertiesTab;
